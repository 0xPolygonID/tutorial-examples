import { ISchemaLoader } from '../loaders/schema';
import { Schema } from '../protocol/models';
export interface Query {
    allowedIssuers: string[];
    req: Map<string, unknown>;
    schema: Schema;
    claimId: string;
}
export interface ClaimOutputs {
    issuerId: string;
    schemaHash: bigint;
    slotIndex: number;
    operator: number;
    value: bigint[];
}
export declare function checkQueryRequest(query: Query, outputs: ClaimOutputs, schemaLoader: ISchemaLoader): Promise<void>;
