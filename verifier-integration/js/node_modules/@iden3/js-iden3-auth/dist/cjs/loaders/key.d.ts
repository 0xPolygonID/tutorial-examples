export interface IKeyLoader {
    load(circuitId: string): Promise<Uint8Array>;
}
export declare class FSKeyLoader implements IKeyLoader {
    readonly dir: string;
    constructor(dir: string);
    load(curcuitId: string): Promise<Uint8Array>;
}
